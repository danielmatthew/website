---
import BaseLayout from "../layouts/BaseLayout.astro";
// import ContactCTA from "../components/ContactCTA.astro";
import Work from "../components/Work.astro";
---
<BaseLayout title="Dan Matthew - UX Engineer">
  <header>
    <h1>Hello, world!</h1>
    <p>I'm a UX Engineer at Talis, an EdTech company part of <a href="https://technologyfromsage.com">Technology from Sage</a></p>
  </header>
  <Work />
</BaseLayout>
<!-- <script>
  const handleSubmit = (event) => {
    event.preventDefault();

    const form = event.target;
    const formData = new FormData(form);

    fetch('/', {
      method: 'POST',
      headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
      body: new URLSearchParams(formData).toString()
    })
    .then(() => console.info('Form successfully submitted'))
    .then(() => form.reset());
  };

  document.forms[0].addEventListener('submit', handleSubmit);
</script> -->
